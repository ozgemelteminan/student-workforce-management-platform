# Email Architecture

Email has a dedicated Infrastructure/Email area and Application abstractions for service/provider boundaries. Provider implementations are intentionally not implemented in the scaffold.
